// Package implementations contains various implementations of interfaces defined in the other places.
// Things like the runner, syscalls and so on, we have interfaces but the real implementations go here.
package implementations
