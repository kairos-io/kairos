name: Update dependencies
on:
  schedule: [{ cron: "0 5 * * 1" }]   # weekly, Monday 05:00 UTC
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Preview without opening a PR"
        type: boolean
        default: false
      model:
        description: "AI model used to update dependencies"
        type: string
        default: "gemma-4-e2b-it"
      agent-prompt:
        description: "Custom instructions for the update agent (replaces the default update task)"
        type: string
        default: ""
      context-size:
        description: "Model context size"
        type: string
        default: "16384"
concurrency:
  group: update-deps
  cancel-in-progress: false
jobs:
  update:
    runs-on: oracle-vm-16cpu-64gb-x86-64   # needs RAM for the AI model
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/create-github-app-token@v2
        id: app-token
        with:
          app-id: ${{ secrets.DEPS_BOT_APP_ID }}
          private-key: ${{ secrets.DEPS_BOT_APP_KEY }}
      - uses: kairos-io/security/.github/actions/update-deps@main
        with:
          language: go
          token: ${{ steps.app-token.outputs.token }}
          dry-run: ${{ github.event.inputs.dry-run || 'false' }}
          model: ${{ github.event.inputs.model || 'gemma-4-e2b-it' }}
          agent-prompt: ${{ github.event.inputs.agent-prompt || '' }}
          context-size: ${{ github.event.inputs.context-size || '16384' }}
