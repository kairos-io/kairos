name: "Custom KUBECONFIG configuration"
stages:
    initramfs:
     - name: "Setup KUBECONFIG"
       files:
       - path: /etc/profile.d/k8s-paths.sh
         content: |
                  export KUBECONFIG=/etc/kubernetes/admin.conf
         permissions: 0755
         owner: 0
         group: 0
