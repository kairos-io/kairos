#cloud-config

install:
  grub_options:
    extra_cmdline: "rd.immucore.debug"

stages:
  initramfs:
    - name: Set user and password
      users:
        kairos:
          passwd: kairos
k3s:
  enabled: true